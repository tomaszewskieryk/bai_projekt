<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.myshopper.repository.FridgeStateRepository">

    <insert id="createFridgeStateForNewUser">
        insert into fridgeStates (fridgeName, isActual, userID)
        values ('actualState', true, #{userID})
        <selectKey resultType="Integer" keyProperty="fridgeStateID" order="AFTER">
            SELECT LAST_INSERT_ID();
        </selectKey>
    </insert>
</mapper>